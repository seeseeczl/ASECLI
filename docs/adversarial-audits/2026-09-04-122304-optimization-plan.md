# 对抗性审计优化计划：截图所示 ASE 车辆材质节点图排版

> 生成时间：2026-09-04 12:23:04 CST
> 项目根目录：`/Users/long/GitHub/ASECLI`
> 关联报告：`docs/adversarial-audits/2026-09-04-122304-audit-report.md`

## 1. 执行摘要

- 审计对象：用户截图所示 ASE 车辆材质节点图。
- Findings 统计：P0=0 P1=2 P2=2 P3=0。
- 发布建议：条件通过；现图可继续作为运行中资产观察，但不应在未先治理主干和跨模块长线的情况下继续叠加功能。
- 建议执行顺序：先取得当前图只读结构清单与 Master 反向依赖；再建立横向鱼骨主干和跨组接口；随后让 LTC 重复分支及所有平行材质层按固定水平层级列对齐；本次不包含真实 ASE 画布、编译或材质验收。

## 2. 修复与加固任务

| 任务 ID | 优先级 | 来源 Finding | 内容 | 验收标准 | 依赖 | 风险/回滚 |
| --- | --- | --- | --- | --- | --- | --- |
| AA-OPT-001 | P1 | AA-LAYOUT-001 | 识别中央橙色跨模块线的生产者、消费者、端口、类型和消费者数量；多消费者/跨组时采用一个语义明确的 Register 与就近 Get，单消费者时改为相邻阶段列。 | `validate` 无新增 error；Register/Get 类型和引用正确；图结构中不再有跨越多个 Comment 的该长线；前后连接 manifest 一致，或唯一新增的是等价 Local Var 接口。 | 当前 `.shader` 图数据 | 不改公式、默认值或端口语义；每次写入保留 `.bak`，异常时恢复原图。 |
| AA-OPT-002 | P1 | AA-LAYOUT-002 | 从 Master 输入反向建立 Base Color、Normal、Metallic、Smoothness、Emission 等最终通道的依赖表，并按“输入→采样/解码→局部计算→层级合成→Master”建立横向鱼骨主干。 | Master 位于最右最终列；每个语义层级只有固定 x 坐标；每根功能骨干水平推进，分支只在上/下展开；所有大模块出口对准同一合成列；前后 `parse` 的节点/连线计数和输出端口语义一致。 | AA-OPT-001 的跨组接口边界 | 先 dry-run 和截图；只移动 x/y 与必要 Comment，禁止借排版修改计算顺序。 |
| AA-OPT-003 | P2 | AA-LAYOUT-003 | 对“清漆 LTC”上/下确实平行的分支套用同一组横向阶段列、节点顺序、上下行距和右侧汇合模板；差异点用短中文 Comment 标题标明原因。 | 两支对应节点位于相同阶段 x 坐标；同级节点垂直对齐；汇合前无无关穿线；单张图即可逐行对照。 | AA-OPT-002 的整体列网格 | 若两分支算法并不等价，不强行镜像；记录差异而保留不同布局。 |
| AA-OPT-004 | P2 | AA-LAYOUT-004 | 将法线/AO/金属度/光滑度与发光相关模块改为脑图式平行支：每块有水平骨干，输入在左、局部计算居中、出口在右；并列模块共用阶段 x 列，而非仅对齐矩形外框。 | 并列 Comment 的左/右边界、标题基线、阶段列与上下分支行距一致；离线 Comment 成员与边界不重叠；空白只服务于向右收束和必要跨支连线。 | AA-OPT-002 | Comment 与成员作为复合单元移动；必要时从 `.bak` 恢复。 |

## 3. 测试补强任务

| 任务 ID | 优先级 | 覆盖缺口 | 内容 | 验收标准 |
| --- | --- | --- | --- | --- |
| AA-TEST-001 | P1 | 截图无法证明实际连接与跨组复用 | 治理前后运行 `asecli parse`、`asecli validate`、`asecli graph-audit`；保存节点/连线 manifest 和 `external_consumers` 结果。 | 无 `DANGLING_WIRE`、重复 ID、Local Var 错误；没有将外部消费者 Property 误删。 |
| AA-TEST-002 | P1 | 仅图审计时的边界与走线证据 | 对 Comment 成员/边界、节点阶段列和 WireConnection 做离线检查；逐条核对 AA-LAYOUT-001 的旧跨组路径已由等价接口替代。 | 无 `COMMENT_GROUP_OVERLAP`；所有普通 DAG 边从左向右推进；没有无语义的跨 Comment 长线。 |
| AA-TEST-003 | P2 | 本次不打开真实画布，不能断言视觉端口曲线或渲染不变 | 将真实画布、recompile 与材质画面验收明确登记为不在本次范围，不用离线结果替代。 | 审计/执行记录明确标注“未验证”；不把图结构通过写成编译或画面通过。 |

## 4. 观测与运维任务

| 任务 ID | 优先级 | 内容 | 验收标准 |
| --- | --- | --- | --- |
| AA-OBS-001 | P2 | 为该图建立一次布局基线：保存 `parse` 摘要、layout/comment dry-run JSON、正常缩放画布截图与材质回归截图，并在后续大改前复用。 | 基线能定位图版本、节点/连线计数、Comment 成员和验收截图；后续改动可对比而非重新凭感觉判断。 |

## 5. 发布门禁

- [ ] AA-OPT-001、AA-OPT-002 完成，并保留不改变 Shader 语义的证据。
- [ ] AA-OPT-003、AA-OPT-004 完成或记录其算法差异导致不适用的理由。
- [ ] AA-TEST-001、AA-TEST-002 通过；AA-TEST-003 已明确登记为本次不验证。
- [ ] 残留风险已记录；无证据项不标为通过，不以图审计替代真实画布、编译或材质结论。

## 6. 未映射项（如有）

- 无。全部 P1/P2 finding 已映射；执行时先做只读结构核对，若截图推断与实际连接不符，应关闭对应任务而不是按图猜改。
