---
id: REL-0012
type: release-record
status: verified
version: 0.6.2
created_at: 2026-09-08T13:49:00+08:00
owner: long
related: [CR-0023, CR-0024, TASK-0058, REG-0057]
supersedes: [REL-0011]
evidence: [Python 3.10 and 3.12 full suites each 341 passed and 3 skipped; regression catalog 59 of 59 parsed; reproducible wheel and sdist; SPDX and supply-chain checks; isolated wheel parse Skill and GUI smoke; REG-0053 isolated Tuanjie evidence]
---

# REL-0012 — ASECLI v0.6.2 公开发布

## 变更集合

- FR/CR/BUG/ADR：`CR-0023` 将 PyPI Trusted Publishing 设为规范安装入口；`CR-0024` 交付 EditorGraphSpec v3 的加性兼容能力。
- 版本/提交/PR：版本 `v0.6.2`；候选提交为本记录所在提交；无 PR，按 solo-maintained 仓库规则由 `main` 直接发布。
- 兼容性说明：Python 保持 `>=3.10`；EditorGraphSpec v1/v2 和文本默认后端保持不变；v3 仅承诺 ASE 1.9.6.2 与已验证 URP Unlit 模板；不迁移或修改既有 Shader。
- 分发范围：公开 GitHub Release、PyPI wheel/sdist、SHA256SUMS、SPDX SBOM 与供应链检查结果；CLI Hub 不在本次范围。

## 验证与风险

- 测试/构建/审计证据：Python 3.10 与 3.12 全量各 `341 passed, 3 skipped`；回归目录 `59 parsed / 6 conditional / 0 missed`；双构建逐字节一致；本地 wheel 版本、parse、Skill 安装/幂等与 GUI 支持安装 smoke 通过；Editor 真实证据见 `REG-0053`。
- 已知风险与监控：GitHub Actions、PyPI OIDC、公开资产匿名回读和本机正式源安装属于远端发布后验，发布失败即停止后续分发；离线供应链检查不等同于实时漏洞数据库。
- Go / No-Go 决定与负责人：本地候选 Go，可进入 `main`、tag 与远端发布门禁；负责人 long。只有远端门禁、资产摘要、匿名回读和正式安装全部成功后，状态才更新为 `released`。

## 回滚

- 触发条件：普通 CI、tag publish workflow、PyPI 上传、GitHub Release 资产校验、匿名下载、本机安装或版本回退演练任一失败。
- 步骤：停止 `v0.6.2` 后续分发，保留失败证据；本机从不可变 `v0.6.1` Release wheel 执行 `uv tool install --force`，恢复原 bundled Skill；公开入口保持或恢复为 `v0.6.1`。
- 验证：隔离环境依次验证 `0.6.2 → 0.6.1 → 0.6.2` 的版本与 parse；本机最终版本、help、Skill 内容与幂等状态必须回读。
- 边界：不删除历史 tag/Release，不覆盖用户不同内容的 Skill，不启动 Editor、不修改生产 Shader。
