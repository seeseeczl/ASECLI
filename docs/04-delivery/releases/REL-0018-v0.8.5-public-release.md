---
id: REL-0018
type: release-record
status: verified
version: 0.8.5
created_at: 2026-09-14T00:00:00+08:00
owner: long
related: [CR-0031, TASK-0067, REG-0064]
---

# REL-0018 — ASECLI v0.8.5

## 变更集合

- FR/CR/BUG/ADR：CR-0031、TASK-0067、REG-0064。
- 版本/提交/PR：v0.8.5 候选；main 直接提交，不移动既有 tag。
- 兼容性说明：SG spec 和公共报告 Schema 不变；CLI 增加精度提示及文件名后缀；保留算法、属性、混合设置检查。精度边界不再误报目标缺少 float 能力。

## 验证与风险

- 测试/构建/审计证据：本地完整回归 485 passed/3 skipped；AUD-0085 无开放 S0/S1；137 节点实文件导出 SHA 核验通过。0.8.5 候选全量 484 passed/4 skipped（私有实样本未设 opt-in），治理、供应链、双构建一致、独立 wheel smoke 与 v0.8.4 隔离回滚 smoke 通过。远端 CI 与正式资产待回写。
- 已知风险与监控：局部 float/half 舍入和可表示范围可能不同；不宣称逐位或视觉等价。Editor/编译/画布/渲染未运行。关注 precision_warnings 与转换报告。
- Go / No-Go 决定与负责人：long 已明确授权推送发布；所有发布检查通过后进入 released。

## 回滚

- 触发条件：正式包不可安装、资产摘要不一致或出现错误算法/混合映射。
- 步骤：`uv tool install --force asecli==0.8.4`；保留 v0.8.4 tag/Release 和用户 Shader，不撤销用户生成文件。
- 验证：隔离安装旧包核对版本及独立 wheel smoke，再恢复新包；正式资产回读后回写结果。
