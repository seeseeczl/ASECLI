---
id: REL-0007
type: release-record
status: in-progress
version: 0.4.1
created_at: 2026-09-05T18:00:00+08:00
owner: long
related: [CR-0006, CR-0017, TASK-0044, REG-0021, REG-0048]
supersedes: [REL-0006]
evidence: [local fast validation, release gates pending]
---

# REL-0007 — ASECLI v0.4.1 私有正式发布

## 变更集合

- FR/CR/BUG/ADR：CR-0006、CR-0017；ADR-0007、ADR-0009；TASK-0044、REG-0021、REG-0048。
- 版本/提交/PR：`v0.4.1` 补丁版本；发布提交、tag、Actions run 和 Release URL 在远端验证后回填；直接推送私有仓库，无 PR。
- 兼容性说明：全部 16 个 CLI 命令与 `tools/call` 的 120 秒执行上限保持兼容；仅缩短 MCP 初始化等待并消除重复验证；`v0.4.0` 保留为回滚点。
- 效率：回归目录正常路径从 40 个串行 pytest 收集进程改为单进程聚合，失败时仍逐项定位。
- CI：全量 pytest 已包含目录治理测试，因此删除其后的重复目录检查步骤。
- MCP：初始化与 initialized 通知使用独立 20 秒建连上限；真实 `tools/call` 仍保留 120 秒执行上限。
- 执行规范：仓库级 `AGENTS.md` 将普通、标准、发布验证分级；Editor 和截图只在相关视觉/Editor 代码最终候选上集中执行。
- 文档：README 公开命令数修正为 16，并补齐 `install-skill --skill-root`。
- 分发：仅私有 GitHub Release，不上传 PyPI、CLI Hub 或公开包仓。

## 验证与风险

- 测试/构建/审计证据：本地定向测试、全量回归、CI governance、回归目录、供应链、Project Architect strict 与可复现构建；远端证据待回填。
- 已知风险与监控：MCP 服务在初始化阶段持续阻塞超过 20 秒时将更早失败；工具执行仍保留 120 秒。监控 CI 时长、建连失败和目录收集数量。
- Go / No-Go 决定与负责人：负责人 long；聚合收集、双 Python CI、正式资产 SHA、本机安装或 `v0.4.0` 回滚任一失败则 No-Go。
- 本地：回归目录 `12.00s → 0.58s`；全量 `26.04s → 16.16s`，`259 passed, 3 skipped`。
- 专项：MCP/bridge 29 项通过；CI governance、回归目录和 `git diff --check` 通过。
- Editor：本版本未改 C# GUI 资源、ASE Editor API 或 Inspector 视觉，按分级规则不启动 Editor、不截图，沿用 `v0.4.0` 对应证据与未验边界。
- 停止条件：聚合检查漏收集、CI 失败、正式资产摘要不一致、本机安装或回滚失败。

## Release 资产

远端 package job 完成后回填 wheel、sdist、SPDX、供应链、漏洞扫描与 `SHA256SUMS`。

## 回滚

- 触发条件：聚合检查漏收集、CI、Release 资产 hash、本机安装、MCP 建连兼容或供应链门禁失败。
- 步骤：停止分发 `v0.4.1`，下载并校验不可变 `v0.4.0` wheel，执行 `uv tool install --force`。
- 验证：确认 `asecli --version` 返回 `0.4.0`，再执行 `asecli --help` 和 `asecli parse`；不修改用户 Shader、材质、Unity/Tuanjie 工程或历史 Release/tag。
