---
id: REL-0009
type: release-record
status: released
version: 0.5.0
created_at: 2026-09-06T13:20:00+08:00
owner: long
related: [CR-0006, CR-0018, TASK-0046, REG-0021, REG-0049]
supersedes: [REL-0008]
evidence: [candidate commit 89883ae, GitHub Actions run 34029422737, GitHub Release v0.5.0, release asset SHA256SUMS, local installation and v0.4.2 rollback]
---

# REL-0009 — ASECLI v0.5.0 私有正式发布

## 变更集合

- 版本：`v0.5.0` minor release；新增公开 CLI 参数、spec 字段和 Editor GUI 能力，`v0.4.2` 保留为不可变回滚点。
- 条件置灰：新增 `EnableIfMzgui(source, operator, value)`，支持 `Less`、`LessEqual`、`Equal`、`NotEqual`、`GreaterEqual`、`Greater`；只通过 `EditorGUI.DisabledScope` 控制可编辑状态，不清空材质值，也不代替 Shader 分支。
- 属性呈现：升级到 `asecli.property-presentation.v2`；中文 Tooltip 必填，GUI 自动追加变量名和 Shader 默认值；HelpBox 是用户可选内容，存在不违规、缺失不阻断，CLI 与 ASE 编辑窗口都可增删。
- Provider：工程已有原生 `MZGUI.MZGUI` 时继续使用并只补 authoring/条件 Drawer；确认缺失时安装同名 fallback。两条路径统一 Foldout/Tooltip/HelpBox/EnableIf 标记，跨工程不改 CustomEditor 名。
- 节点图治理：双扇出或像素长度只进入候选；按模块边界、跨阶段/遮挡和消费者离散度决定直连、Local Var 或混合模式。
- 分发：仅私有 GitHub Release，不上传 PyPI、CLI Hub 或公开包仓；不批量修改用户 Shader、材质或 Editor 工程。
- 发布对象：[ASECLI v0.5.0（内部正式版）](https://github.com/seeseeczl/ASECLI/releases/tag/v0.5.0)，非 Draft、非 Prerelease，并设为 Latest；annotated tag 解引用到候选提交 `89883ae185af8f2c1d2a56133f5a372f3e9c8be7`。

## 发布门禁

- 本地：Python 3.10/3.12 各 `276 passed, 3 skipped`；回归目录 `51 parsed / 6 conditional / 0 missed`；CI governance、供应链、lock 与 diff 检查通过。
- 构建：固定 epoch 的仓库外双构建逐字节一致；wheel `93a4ff0…440d0`、sdist `e7266bc…0acb3`；隔离 wheel 返回 `asecli 0.5.0`，fallback 安装 smoke 与包内 Skill/C# 条件资源通过。
- 远端：[GitHub Actions run 34029422737](https://github.com/seeseeczl/ASECLI/actions/runs/34029422737) 的 Python 3.10、Python 3.12、package 三项 job 全绿；artifact `9988107642` digest 为 `a74d6fb…58864`。
- 资产：六项 Release 资产重新下载；五项载荷均通过 `SHA256SUMS`，且 GitHub asset digest 与本地复算一致。SPDX 名称为 `asecli-0.5.0`；供应链零 finding/零运行时依赖；在线 `pip-audit` 无已知漏洞并明确记录专有包本体无法从 PyPI 审计。
- 安装：正式 wheel 已安装到 `/Users/long/.local/bin/asecli` 并返回 `0.5.0`；条件参数、六种比较符、fallback 安装和 bundled Skill 幂等通过；隔离环境完成 `0.5.0 → 0.4.2 → 0.5.0`，三阶段 parse 均成功。
- Editor 边界：本次不启动 Unity/Tuanjie、不截图；自动与资源级验证不能表述为真实 Inspector 编译或视觉通过。

## Release 资产

| 文件 | SHA-256 |
| --- | --- |
| `asecli-0.5.0-py3-none-any.whl` | `93a4ff0e46a7439fbecfe5eba0e0a3301db18b564b14e2cb196f2025439440d0` |
| `asecli-0.5.0.tar.gz` | `e7266bc63b7aa0dbd3b4ec0a3a28b3df92fc4650506b4ffad48a05d82060acb3` |
| `asecli-0.5.0.spdx.json` | `3d23a2bb1530f4d4cc7585b9fb9d072ef4336d773a118418fb1b6577aa01b6dd` |
| `supply-chain-check.json` | `2c44e2b494c82463847530f309175e3144f813f92f44bf42d57f57813e220e3f` |
| `vulnerability-scan.json` | `1df6b51015d0c9bb5c0578927ccbdfa29b08660f8309d9dfbb9d9f6fde2206e5` |
| `SHA256SUMS` | `bd29dc87785a0200cc39d6b3f4ef3e460c8470fd5f7061e33579c31244ed9e49` |

## 回滚

- 触发条件：Tooltip/HelpBox 契约、provider 唯一性、条件元数据、CI、资产摘要、安装或 Skill 任一门禁失败。
- 步骤：停止分发 `v0.5.0`，下载并校验不可变 `v0.4.2` wheel，执行 `uv tool install --force`。
- 验证：隔离 Python 3.12 环境已确认 `0.5.0 → 0.4.2 → 0.5.0` 三阶段版本与 parse；不修改用户 Shader、材质、Editor 工程或历史 Release/tag。
