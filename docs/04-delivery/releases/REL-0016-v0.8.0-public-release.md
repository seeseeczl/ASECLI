---
id: REL-0016
type: release-record
status: verified
version: 0.8.0
created_at: 2026-09-11T18:57:39+08:00
owner: long
related: [CR-0030, ADR-0022, TASK-0065, REG-0063, AA-OPT-301, AA-OPT-302, AA-OPT-303, AA-OPT-304, AA-OPT-305, AA-OPT-306, AA-OPT-307, AA-OPT-308, AA-OPT-309]
supersedes: [REL-0015]
evidence: [local dual-Python regression, reproducible candidate artifacts, clean release-mode cross-CLI matrix]
---

# REL-0016 — ASECLI v0.8.0

## 变更集合

- 版本：`0.8.0`；双向创建规格升级为对方可直接消费的裸 JSON，固定方向化 spec/report/receipt 命名，不恢复 wrapper、中间交接包或一次性 translator。
- 门禁：public PR 不读取私有仓库；受信 main/Tag 使用 SGCLI 专用只读 Deploy Key 与精确 commit SHA。package wheel 原样交给 cross-CLI 与 publish。
- 证据：固定双向 WaveNoise 语义 fixture；report/spec/receipt 绑定；fixture-set 完整性；Draft 2020-12 数值等价；clean provenance。

## 发布候选证据

- Python 3.10/3.12 全量均为 `428 passed, 3 skipped`；回归目录 `63 parsed / 6 conditional / 0 missed`；CI governance、lock 与 diff 检查通过。
- 本地可复现构建：wheel `d5401d357f05811e41ad57e2c747298a02eb8ca5e4d3c51595fba37a28acbbd5`；sdist `2d80cdf9f317afd7ccf322b8ed9abf151e039dcef09dfe3ca89769f77e57c2f6`。
- SGCLI 0.3.0 与 ASECLI 0.8.0 候选 wheel 在临时干净快照完成 `mode=release` 双向矩阵：两仓 `dirty=false`、WaveNoise gate configured、Schema 字节一致、篡改 wheel 被拒绝、离线 verifier 通过。
- 远端 CI、PyPI OIDC、GitHub Release、正式资产回读和本机安装在 Tag 后补录；完成前状态保持 `verified`。
- Editor 创建/保存重载/编译、真实画布与同条件渲染不由本次 Python/CI 门禁替代；发布后仍保持公开边界，不声明视觉等价。

## 回滚

- 保留不可变 `v0.7.0`。如跨仓门禁、资产摘要、官方安装或双向消费失败，停止推广并执行 `uv tool install --force asecli==0.7.0`，再恢复安装前 Skill 备份。
