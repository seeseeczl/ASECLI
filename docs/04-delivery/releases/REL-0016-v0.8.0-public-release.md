---
id: REL-0016
type: release-record
status: released
version: 0.8.0
created_at: 2026-09-11T18:57:39+08:00
owner: long
related: [CR-0030, ADR-0022, TASK-0065, REG-0063, AA-OPT-301, AA-OPT-302, AA-OPT-303, AA-OPT-304, AA-OPT-305, AA-OPT-306, AA-OPT-307, AA-OPT-308, AA-OPT-309]
supersedes: [REL-0015]
evidence: [local dual-Python regression, reproducible candidate artifacts, clean release-mode cross-CLI matrix, tag publish run, GitHub and PyPI asset parity, installed CLI and Skill smoke]
---

# REL-0016 — ASECLI v0.8.0

## 变更集合

- FR/CR/BUG/ADR：CR-0030、ADR-0022、TASK-0065、REG-0063；第二/三轮对抗审计 AA-OPT-201～207 与 AA-OPT-301～309 已实施，远端 ruleset/release environment 仍按审计边界独立披露。
- 版本/提交/PR：`v0.8.0@58ccf1094159cd884f87deb4f1a7fbffd3e4148f`；main 直接提交，新 annotated tag，无 PR，未移动旧 tag。
- 兼容性说明：ASECLI 直接输出 SGCLI 可消费的裸 `sgcli.native.v3`，并直接消费 SGCLI 输出的裸 EditorGraphSpec v3；固定方向化 spec/report/receipt 命名。wrapper、中间交接包、反方向文件和一次性 translator 不再进入主链；未知语义失败关闭。

## 验证与风险

- 测试/构建/审计证据：Python 3.10/3.12 全量均为 `428 passed, 3 skipped`；回归目录 `63 parsed / 6 conditional / 0 missed`；CI governance、lock、diff、可复现构建、SPDX 和供应链检查通过。SGCLI 0.3.0 与 ASECLI 0.8.0 在干净快照完成 `mode=release` 双向矩阵，包含 WaveNoise、Schema 字节一致、篡改拒绝和离线 verifier。
- 远程发布：[publish run 34592028675](https://github.com/seeseeczl/ASECLI/actions/runs/34592028675) 的 Python 3.10/3.12、package、public contract、只读 Deploy Key cross-CLI 和 PyPI OIDC 全部成功。[GitHub Release v0.8.0](https://github.com/seeseeczl/ASECLI/releases/tag/v0.8.0) 非 Draft/Prerelease，包含 wheel、sdist、SHA256SUMS、SPDX 和供应链报告五项资产。
- 双渠道回读：GitHub Release 资产重新下载后与 SHA256SUMS/GitHub digest 一致；PyPI `asecli==0.8.0` 的 wheel SHA-256 为 `d5401d357f05811e41ad57e2c747298a02eb8ca5e4d3c51595fba37a28acbbd5`，sdist 为 `2d80cdf9f317afd7ccf322b8ed9abf151e039dcef09dfe3ca89769f77e57c2f6`，与 build-once artifact 完全一致且两者 `yanked=false`。
- 本机安装：官方 PyPI `asecli==0.8.0` 已通过 `uv tool` 安装，`--version` 与双向 WaveNoise 生产/消费 smoke 通过。`.agents`/`.claude` Skill 与 wheel 逐文件一致，digest 为 `eae8ad6b74c7d940f32e9e4be351cabdff55a380fb7efbe90a5ac7ed9e9e1d34`；旧三处 Skill 完整备份于 `/Users/long/.codex/skill-backups/20260911-190447-pre-asecli-v0.8.0`。ASE→SG 规格 SHA-256 为 `80b4307ae912c9ccede5e9b5f1ffae0dbb06a07a73a1aad478a5c2c65e23c6d2`，SG→ASE 为 `6f527d09334912ba3e839b37733dad66fe4cb7bcb6b86db0d958c2d230de6cec`，两个消费回执均一致。
- 已知风险与监控：本次最终 wheel 没有运行 Editor 创建/保存重载/编译、真实画布或同条件渲染；上述层级不由 Python/CI 门禁替代，发布不声明视觉等价。
- Go / No-Go 决定与负责人：long 已授权双 CLI 推送、发布和本机安装；代码门禁、跨仓直连、OIDC、资产回读和安装均通过，Go；视觉等价保持 No-Go/未验证。

## 回滚

- 触发条件：跨仓契约漂移、发布资产摘要不一致、官方包无法安装、反方向/wrapper 被错误接受，或双向原文件 SHA 不一致。
- 步骤：停止推广并执行 `uv tool install --force asecli==0.7.0`；保留不可变 `v0.7.0` tag/Release，不删除用户 Shader。如需恢复 Skill，从 `/Users/long/.codex/skill-backups/20260911-190447-pre-asecli-v0.8.0` 按原目录还原。
- 验证：回滚后检查 `asecli --version`、旧契约命令与 Skill 内容；新版修复后重新走完双 Python、cross-CLI、可复现构建、PyPI OIDC 和资产回读，不覆盖旧 tag。
