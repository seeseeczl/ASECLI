---
id: REL-0003
type: delivery-evidence
status: released
version: 0.2.0
created_at: 2026-09-02T21:45:00+08:00
owner: long
related: [CR-0006, CR-0012, CR-0013, FR-0005, FR-0010, FR-0011, BUG-0016, BUG-0017, BUG-0018, TASK-0032]
supersedes: []
evidence: [GitHub Actions run 33637146781, GitHub Release v0.2.0, release asset SHA256SUMS]
---

# REL-0003 — ASECLI v0.2.0 私有正式发布

## 范围

- 发布提交：`6865aaa59e5a90fa10bc583cd10c7dd191444ea5`（`main`）。
- 正式标签：`v0.2.0`，固定指向上述发布提交。
- 发布对象：[ASECLI v0.2.0（内部正式版）](https://github.com/seeseeczl/ASECLI/releases/tag/v0.2.0)，状态为非 Draft、非 Prerelease。
- 分发形式：私有 GitHub Release 中的 Python wheel；不上传 PyPI、CLI Hub 或其他公开包仓。
- 未覆盖 `v0.1.0` 资产；`v0.1.0` 仍可下载并回滚安装。
- 新 Editor 进程重开未执行，不作为本 Release 的已验证项。

## 正式发布门禁

- [GitHub Actions run 33637146781](https://github.com/seeseeczl/ASECLI/actions/runs/33637146781)：发布提交的 Python 3.10、Python 3.12、package 三个 job 全绿。
- 发布前对最终 wheel 的隔离环境执行在线 `pip-audit`：运行时依赖为 0，未发现依赖漏洞；专有 `asecli` 本身不在 PyPI 漏洞数据库中，因此按工具语义跳过，不把跳过项误报为已扫描。
- Release 上传完成后重新下载全部六个资产；`SHA256SUMS` 中登记的五个载荷资产执行 `shasum -a 256 -c SHA256SUMS` 全部通过，清单文件自身再与 GitHub 返回的资产摘要核对。

## Release 资产

| 文件 | SHA-256 |
| --- | --- |
| `asecli-0.2.0-py3-none-any.whl` | `5dd89f11d760a1d7a7e704213f140ce649c16d3e4513829c83842438c26d954e` |
| `asecli-0.2.0.tar.gz` | `112ec8368c520181786b30bfad6e50a0025c7ff31f5a8c9dccc91812cd2a779a` |
| `asecli-0.2.0.spdx.json` | `bf0b0937aca37eeb3d42d618fc9b750e1ab39a3726a7c6884eda3a427eebf9f8` |
| `supply-chain-check.json` | `2c44e2b494c82463847530f309175e3144f813f92f44bf42d57f57813e220e3f` |
| `vulnerability-scan.json` | `87b8b6a2ed1cf98ddb395969c074495e7ed2149897c07778f0052effe75cb8cf` |
| `SHA256SUMS` | GitHub 资产摘要 `be4bf4389a7d4f642763ae83f4380b3d200c0a7c3b2b46b2946decb9c0c58b03` |

从正式 Release 下载 wheel 后，在全新 CPython 3.12 隔离环境安装，包版本为 `0.2.0`，`asecli --help` 成功列出全部 15 个公开子命令。

## 试用者安装

安装者需先获得私有仓库权限，并完成 GitHub CLI 登录：

```bash
gh auth login
mkdir asecli-v0.2.0
cd asecli-v0.2.0
gh release download v0.2.0 --repo seeseeczl/ASECLI
shasum -a 256 -c SHA256SUMS
uv tool install ./asecli-0.2.0-py3-none-any.whl
asecli --help
```

## 隔离回滚观察

正式发布前，在临时 CPython 3.12 环境中先安装正式 `v0.1.0` wheel（SHA-256 `8d19a0abe2f9083acaac6a3c1801cc5da38ba2c6a0aa64136d352667ec202318`），随后执行：

1. 安装 `v0.1.0` wheel，包版本为 `0.1.0`，`asecli parse tests/fixtures/step-antialiasing.function.txt` 成功。
2. 强制重装本次 `0.2.0` wheel，包版本为 `0.2.0`，重复 parse 成功，`--help` 列出 15 个子命令。
3. 强制重装 `v0.1.0` wheel，包版本回到 `0.1.0`，重复 parse 成功。

该演练只在临时环境发生，不写入用户团结工程，也不改动任何用户 Shader。

## 边界

- 本记录证明 `v0.2.0` 已作为可安装的正式 CLI，通过私有 GitHub Release 完成交付，并具备 CI、哈希、SBOM、供应链、漏洞扫描和安装/回滚证据。
- 本次发布仍使用 `LicenseRef-ASECLI-Proprietary`。只有获得私有仓库权限的授权试用者可以下载；不得公开转发源码或 Release 资产。
- 未创建 PyPI、CLI Hub 或其他公开包仓条目；开源、公开分发、代码签名和后续版本发布仍需单独授权与门禁。
- Editor v2 新进程重开仍未执行，不纳入本 Release 已验证范围。
