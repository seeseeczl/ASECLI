# 项目优化任务书

## 执行原则

- 本任务书对应 2026-09-01 13:25:48 增量复审，只记录当前开放整改任务，不重复执行已关闭的基线任务。
- 证据边界继续区分本地、真实隔离 Editor、目标平台与远程状态；没有远程 run/link 时不得标为通过。
- 后续若出现新问题，必须由新的增量审计或 BUG/REG 记录承接，不在本任务书中顺手扩大范围。
- 默认复用现有 Python/uv/pytest/Tuanjie/MCP 技术栈；不创建 commit、不 push、不发布远程 Release。

## 总目标

- 证明基线 14 个问题已在本地授权范围内关闭，并在没有开放问题时停止新增实现任务。

## 范围

- 同时间戳审计报告覆盖的 145 个文件、9 个功能模块、11 个 CLI 入口、双 Python 回归、隔离 Tuanjie/MCP E2E、本地发布候选与供应链证据。

## 非目标

- 不把远程 GitHub CI/Dependabot/Release、Windows/Linux、签名或公证的未验证状态转写为通过；不为这些边界创建未经授权的远程任务。

## 问题到任务映射

本次审计无开放问题，因此无问题到任务映射。基线 14 个问题均由既有 P1.1～P1.11、P2.1～P2.2 完成并在增量报告逐项列为已解决。

| 问题 ID | 严重程度 | 优先级 | TODO ID | 处理方式 |
| --- | --- | --- | --- | --- |
| 不适用 | 不适用 | 不适用 | 不适用 | 无开放问题，不创建重复任务 |

## TODO

无优化任务。当前开放问题数为 0；远程未验证边界等待用户另行授权，不属于本轮可执行整改。

## 任务详情

无优化任务，因此没有可执行任务详情。既有整改任务的完整目标、范围、失败优先回归、命令、风险、回滚和证据回写保留在 `2026-09-01-122217-optimization-tasks.md`，本次不复制或重编号。

## 进度更新模板

```markdown
## TODO

无优化任务。

当前进展：
- 已完成：基线问题整改与增量复审
- 正在做：不适用
- 下一步：仅在获得远程授权或出现新问题时建立新的增量记录
- 阻塞/风险：远程 CI/Release 未验证，但不阻塞本地整改闭环
```

## Definition of Done

- [x] 所有 S0/S1 和 P0/P1 开放问题均已映射；本次开放问题为 0，因此无重复 TODO。
- [x] 已按 `.project-architect.json` 分类阈值执行 LOC/fitness，扫描未截断，超限项为 0。
- [x] 回归覆盖受影响模块和关键链路，真实隔离 Tuanjie/MCP 成功与失败路径均有脱敏证据。
- [x] OpenSpec 与 CodeGraph 未初始化的原因已说明，Product Design 因无 UI/视觉目标不适用。
- [x] 需求变更、技术决策和实际修改文件已通过 CR/ADR/BUG/REG/TASK/REL/timeline/traceability 增量留档。
- [x] 审计范围内每个功能模块均完成需求→入口→主路径→数据/状态→异常恢复→可观测性→回归→发布证据闭环对账。
- [x] 审计范围内每个前端入口（本项目为 11 个 CLI 入口）均有成功、失败与恢复结论。
- [x] 七类专项工程审计均有证据状态，当前没有需要映射的 P0/P1 专项问题。
- [x] 请求/实际审计模式和降级边界已说明，未把远程或未运行平台当作通过。
- [x] 增量对比已逐项分类新增、已解决、复发或回归、改善和未变化。
- [x] 计划外发现仅记录 sdist 自引用风险及其最小排除策略，没有扩大实现范围。
