# AUD-0085 — v0.8.5 发布前复核

范围：CR-0031 的 Python 导出、报告、CLI 命名、属性对账及技能说明；未修改 Editor 或其他仓库。

- S0/S1：本次复核未发现未解决项。
- 算法表达式和 Alpha 来源匹配后才折叠；RGB/Alpha 混合无法证明时保持阻断。
- 精度证据不再生成 semantic blocker；图精度 Single 保留；其他消费者仍保留所需计算。
- 变量名、类型、默认值、范围与隐藏/HDR 标记对账；不能映射的运行时属性标记按字段报错。
- 输出后缀限制为安全字符；三文件保持原子 create-if-absent 和 SHA 绑定；源文件未写。
- 定向 58 passed；全量 485 passed/3 skipped；实样本 137 节点、150 连接、15 属性，63 个剩余 Custom Function 载荷摘要一致。
- 已知边界：数学等价不代表逐位等价；局部 half 转换可能影响舍入或可表示范围。Editor/编译/视觉未运行，不作发布前置。
- 发布候选仍需 tag CI、双构建一致性、正式资产 SHA 与隔离回滚安装；在完成前不标记 released。
