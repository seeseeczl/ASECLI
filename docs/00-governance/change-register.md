---
id: GOV-CHANGE-REGISTER-0001
type: change-register
status: in-progress
version: 1.1.0
created_at: 2026-09-01T12:54:38+08:00
owner: long
related: [AUD-20260901, ARCH-REQ-0001, PLAN-0001]
supersedes: []
evidence: [docs/05-audits/2026-09-01-122217-optimization-tasks.md]
---

# 变更登记 — 2026-09-01 审计整改

| CR ID | 修改语义 | 影响模块/API | 兼容与迁移 | 回归 | 发布/回滚 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| CR-0002 | schema add-node 增加 ASE 版本门禁；create 仅替换 donor graph；所有图写入执行结构门禁 | MOD-CORE MOD-SCHEMA MOD-CHECK MOD-CLI | raw `--line` 保留；不兼容 schema 拒绝写入 | REG-0013 REG-0014 REG-0015 | 回滚到禁止 add-node/graph-from；文本只读能力保留 | 自动已验证，Editor 待验 |
| CR-0003 | argparse 失败统一单行 JSON；validate error 返回 exit 2；fix-checksum 默认 dry-run | MOD-CLI MOD-CHECK | 调用方需为 fix-checksum 增加 `--write`；help 仍为文本 | REG-0004 REG-0007 REG-0018 | 回滚单个 CLI 入口，不改变 checksum 算法 | 已验证 |
| CR-0004 | MCP tool error/保存语义严格判定；默认仅 loopback；token 改由环境注入；禁止重定向 | MOD-BRIDGE MOD-CLI MOD-SKILL | 远程端点需 `--allow-remote-mcp`；拒绝 argv token | REG-0016 REG-0017 | 可完全禁用远程，仅保留 loopback | mock 已验证，真实 MCP 待验 |
| CR-0005 | core 新增公开 `parse_node_line`，CLI 停止使用私有 helper | MOD-CORE MOD-CLI | `_parse_node_line` 保留一个兼容周期 | REG-0019 | 恢复旧 alias 调用，不删除公开 API | 已验证 |
| CR-0006 | 建立双 Python CI、可复现 artifact、内部许可、离线供应链检查、SPDX SBOM 与本地回滚 | MOD-CLI 交付链 | 不自动发布；远程 run 未发生时不得称为通过 | REG-0021 | 删除 CI 配置不影响运行时；恢复最后已验证 wheel | 本地与 GitHub artifact 已验证；`v0.1.0`、`v0.2.0`、`v0.3.0`、`v0.3.1`、`v0.4.0` 私有正式 Release 的 CI、五项载荷哈希和清单摘要通过；REL-0002/0003/0004/0005/0006 released |
| CR-0007 | FR-0010 增加节点复用治理：跨区域或多消费者结果优先使用 Register/Get Local Var，Comment 管算法边界、Local Var 管模块数据接口 | MOD-SKILL | 不改变 CLI/API；一次性相邻链路继续直连；Register schema 不安全时必须经真实 ASE 创建或复用同版本样本 | REG-0010 | 回滚文档规范即可，不改现有 Shader 或运行时 | 文档与真实参考静态证据已验证；目标图应用待实际任务 |
| CR-0008 | FR-0011 增加受控 ASE Editor API 创建后端：声明式规格驱动模板、Sampler、CustomExpression、连线、Save→Load 回读；静态安全节点继续走离线文本链路 | MOD-BRIDGE MOD-CLI MOD-SKILL | `create` 现有文本模式保持兼容；Editor/auto 为 additive opt-in；不支持版本失败关闭 | REG-0026 REG-0027 REG-0028 REG-0029 | 禁用 Editor 后端即可回退到模板壳创建；不迁移已有 Shader | 已验证（结构创建；目标画面待验） |
| CR-0009 | 执行 `2026-09-01-203438` 对抗性审计计划：可复现构建隔离、精确 ASE 版本矩阵、JSON-RPC ID、后验失败保留、GUI unknown/反射门禁 | MOD-CUSTOM-GUI MOD-GUI-SUPPORT MOD-BRIDGE MOD-CLI 交付链 | 未知 ASE 版本从猜测兼容改为拒绝；post-commit 后验失败从自动删除改为保留诊断；gui-support 新增可选 `--runtime-probe` | REG-0031 REG-0032 REG-0033 REG-0034 REG-0035 | 可分别回滚；AA-OPT-003 回滚仅允许继续保留，不恢复危险 unlink | 自动与 GitHub run/artifact 已验证；真实 GUI probe/Inspector/渲染待验 |
| CR-0010 | 自定义材质 GUI 元数据从 MZGUI-compatible 属性和原生提供者选择，迁移为 ASECLI 自有通用标记与唯一内置提供者 | MOD-CUSTOM-GUI MOD-GUI-SUPPORT MOD-BRIDGE MOD-CLI MOD-SKILL | 新写入 `ASECLIFoldout`/`ASECLITooltip`/`ASECLIHelpBox`；旧三标记只读兼容，触碰即迁移；无属性 API 且原生同名旧 drawer 存在时，仅从同一 `Assets/` 源 Shader 补齐缺失元数据；删除原生探测、优先级和 `gui-support --runtime-probe` | REG-0022 REG-0023 REG-0030 REG-0032 REG-0035 | 回滚仅恢复上一版 CLI/资源；不批量修改用户 Shader 或删除已安装资源 | 定向 67 passed/1 skipped；Python 3.10/3.12 全量均 203 passed/3 skipped；团结 2022.3.61t9 Inspector 与用户 Tooltip 截图通过；`v0.1.0@fbd9941` 的 CI run 33594698477 全绿，私有正式 Release 五项载荷哈希和清单摘要、隔离安装与上一版回滚通过；REL-0002 released |
| CR-0011 | 将常驻说明的目标视觉从人工约定提升为 ASECLI 可查询、写前强制的版本化呈现契约 | MOD-GUI-SUPPORT MOD-BRIDGE MOD-CLI | `gui-support` JSON additive 新增 `capabilities.inline_help_presentation`；`--write` 在内置资源违反 `asecli.inline-help.v1` 时拒绝安装；不改现有 Shader 元数据或材质值 | REG-0036 | 可回滚 Python 契约模块与 C# 轻量渲染；已安装工程文件由其原内容哈希识别，不自动覆盖 | 先失败 2 项；全量 208 passed/3 skipped；当前团结 2022.3.61t9 Inspector 与 Console 通过；待后续发布 |
| CR-0012 | 将公开属性的中文显示名、自动技术 Tooltip 和中文使用说明从建议提升为所有 ASECLI 创建文件的强制契约 | MOD-CUSTOM-GUI MOD-BRIDGE MOD-CLI | 新增 `asecli.property-presentation.v1`；`custom-gui` 公开逐属性状态，ASECLI-managed 写入不得破坏契约；文本创建只接受合规组合，Editor CLI 创建升级到 v2，v1 保留底层桥接兼容；MCP 3.4.7 envelope/安全扫描/回执时序兼容；CI 包路径、SBOM 版本和可移植校验清单随项目版本生成 | REG-0037 REG-0038 REG-0039 REG-0040 | 恢复本 CR 前 CLI 可回滚；已创建文件不自动迁移，非 ASECLI-managed 旧文件仍可只读/普通维护 | 新用例先失败 5 项；双 Python `223 passed, 3 skipped`，REG 36 条、CI governance、供应链、Skill、可复现 0.2.0 构建与隔离安装/回滚通过；两个既有 Shader 共 7 属性零违规；当前团结 v2 创建、暂存重载、提交、独立 recompile 后两属性零差异；Inspector 中文显示名/说明可见，用户现场确认两项 Tooltip 正常；验证资产已精确清理；首次推送先后暴露历史版本硬编码和下载清单路径前缀，已登记 BUG-0017/0018 并修复；`v0.2.0` 已作为私有正式 Release 发布，新进程重开受限未执行 |
| CR-0013 | Editor `create` 成功 JSON 区分暂存图重载与已提交目标图重载，避免 Agent 把 `reloaded=true` 理解成目标图已在 ASE 中重开 | MOD-BRIDGE MOD-CLI MOD-SKILL | additive 增加 `staging_reloaded` 与 `target_graph_reloaded`；保留 `reloaded` 作为暂存重载兼容别名；同一次创建调用仍不 Load 已提交目标图 | REG-0038 | 去掉新字段即可回退到仅 `reloaded` 兼容别名；不得恢复提交后立刻 Load 目标图 | 失败优先契约测试先暴露缺字段；修复后 editor_create/CLI 回归断言 `reloaded=true`、`staging_reloaded=true`、`target_graph_reloaded=false`；随 `v0.2.0` 私有正式 Release 交付 |
| CR-0014 | 打包 C# 改为按显式顺序确定性拼装，运行时仍是单文件/单 payload；`gui-support` 只对已知旧哈希开放可恢复原子升级 | MOD-BRIDGE MOD-GUI-SUPPORT | 现有安装与 Editor execute_code 内容逐字节不变；已知 `0.2.0-original` 返回 `asecli_upgrade_available`，写入前备份并复核；未知内容、符号链接、冲突备份和并发变化仍失败关闭 | REG-0044 REG-0045 | 回滚到单资源读取并恢复 EXC-0001/0002；升级失败从确定性备份恢复，不覆盖未知目标 | 自动 35 项通过；团结 `2022.3.61t9` GUI 升级编译 1 passed/26.15s，ASE `1.9.6.2` 创建后全新进程重载 1 passed/39.86s；两项 LOC 例外已删除；随 `v0.3.0` 私有正式 Release 交付 |

| CR-0015 | 将 ASE 节点图精排与材质 Inspector 编排规范固化为随 wheel 分发的 `asecli` Agent Skill；新增受控 `install-skill` 安装命令 | MOD-CLI MOD-SKILL | 新增 CLI 入口；Skill 安装仅写 `$CODEX_HOME/skills/asecli` 或 `~/.codex/skills/asecli`，同内容幂等、不同内容拒绝覆盖；不改用户 Shader、材质或现有自定义 Skill | REG-0046 | 回滚到 `v0.3.0`；已安装 Skill 可保留或由用户手动移除，不影响 Shader 数据 | [run 33833342070](https://github.com/seeseeczl/ASECLI/actions/runs/33833342070)、正式 wheel/五项载荷哈希、隔离和本机 `uv tool install` 后 Skill 安装通过；随 `v0.3.1` 私有正式 Release 交付（REL-0005） |
| CR-0016 | 恢复完整 MZGUI 公共协议与原生 provider 优先；无 MZGUI 时安装同名 ASE Editor authoring fallback | MOD-CUSTOM-GUI MOD-GUI-SUPPORT MOD-BRIDGE MOD-CLI MOD-SKILL | 统一 `MZGUI.MZGUI` 与三种 canonical Attribute；旧名可视化迁移；原生 authoring 状态协调；Apply 事务；V2 多 provider 探测；可恢复 authoring-only handoff | REG-0047 | 已知哈希备份/原子恢复；未知文件、字段、provider 冲突均失败关闭 | `258 passed, 3 skipped`；治理全绿；真实 native probe 和 authoring-only 内存编译通过；[run 33957105604](https://github.com/seeseeczl/ASECLI/actions/runs/33957105604) 与 `v0.4.0` 私有正式 Release 资产/安装/回滚通过（REL-0006）；无 MZGUI UI 保存重开仍待验 |
| CR-0017 | 将普通改动改为分级验证；回归目录将所有 pytest 片段解析为 selector 后单进程收集，CI 用显式快速步骤展示覆盖计数；MCP 初始化共用 20 秒总预算，工具执行仍为 120 秒 | MOD-CLI MOD-BRIDGE | 保持 CLI 与公开 `McpClient` 旧四位置参数兼容；超时统一为结构化 `BRIDGE_ERROR`；目录 parser 必须报告 parsed/conditional/missed 且遗漏为零 | REG-0048 | 回滚 AGENTS、checker、CI 显式步骤和 connect deadline；不改 Shader、GUI 资源或 Editor 数据 | v0.4.1 初始优化为目录 `12.00s → 0.58s`、全量 `26.04s → 16.16s`；发布后对抗审计复现位置参数错位、9 个目录片段漏检和裸 `TimeoutError`，当前补强为 `50 parsed / 6 conditional / 0 missed`、旧位置参数兼容、真实停滞 socket 与总 deadline 回归；未启动 Editor 或截图 |
| CR-0018 | 将材质属性契约升级为 Tooltip 必填、HelpBox 用户可选，并把 `EnableIfMzgui` 条件置灰贯通 CLI、ASE PropertyNode、原生 MZGUI 扩展与 fallback Editor authoring | MOD-CUSTOM-GUI MOD-GUI-SUPPORT MOD-BRIDGE MOD-CLI MOD-SKILL | 公共 Editor 名保持 `MZGUI.MZGUI`；原生 provider 存在时仅安装 authoring/条件扩展，缺失时安装同名 fallback；旧 HelpBox 数据继续保留且可编辑，不再作为门禁；条件仅禁用控件、不清空材质值 | REG-0049 | 回滚到 `v0.4.2`；不批量改写用户 Shader，不覆盖未知 provider/资源 | 双 Python `276 passed, 3 skipped`；run 34029422737 三项 job、v0.5.0 六项资产、本机安装和 v0.4.2 回滚/恢复通过；未启动 Editor/截图；REL-0009 released |
| CR-0019 | `layout` 增加 opt-in `meticulous`：使用单次 ASE 几何快照，将 DAG 转成主树与次级边，以 Output 为根递归居中上游子树，并联动收紧 Comment 与输出 V2 审计 | MOD-LAYOUT MOD-BRIDGE MOD-CLI MOD-SKILL | `legacy` 仍为默认且保留 `--gap-x/--gap-y`；精排几何或硬门禁失败不写盘；只允许节点坐标和 Comment bounds 变化，不自动改写 Local Var | REG-0050 | 禁用 `--mode meticulous` 即回退，不迁移已有 Shader；`.bak` 保留写前内容 | 自动回归与集中实机验收通过；`v0.6.0@a54795d` 已通过 run 34033313137 发布为私有正式 Release，六项资产摘要、本机 CLI/Skill 安装和隔离回滚验证通过；最终视觉仍按每个目标文件返回 `requires_editor_review` |
| CR-0020 | Local Var 治理改为按去重后的消费组数量判定：同一节点输出或算法结果被两个及以上不同组消费时必须注册，同组内无论复用多少次都允许直连；直连仍须尽量消除交叉和重叠 | MOD-LAYOUT MOD-SKILL | `asecli.graph-layout.v2.remote_direct_wires` 使用 Comment 归属识别多组消费，只报告生产者组外的直接连接；不自动创建或改写 Local Var；线路先通过节点/子树与已有 WireNode 整理，任务允许结构调整时可增加最少 WireNode 锚点 | REG-0050 | 回滚 `_remote_direct_wires` 与 Skill/README/架构规则；不修改用户 Shader | 定向布局/Comment/bridge 回归 `53 passed`；同组三次直连不报告，两个消费组只报告跨出生产者组的直连；线线交叉继续由 V2 列出并要求最小化；已随 `v0.6.1` 私有正式发布 |
| CR-0021 | 将 `meticulous` 原地升级为递归局部鱼骨：真实端口水平、同层输出侧右对齐、中位主骨上下展开；保留并收紧 Comment，补齐可可靠推断的作用标题；新增显式 `--route-wires` Editor 路由事务 | MOD-LAYOUT MOD-BRIDGE MOD-CLI MOD-SKILL | `legacy` 不变；默认不移动/新增 WireNode；显式路由每条最多新增 2 个锚点且必须减少穿越或交叉；折叠锚点后逻辑边完全等价；Comment ID/成员/嵌套/颜色保持；失败恢复 `.bak` | REG-0051 | 回滚到 `v0.6.0` 或使用 `--mode legacy`；不修改生产 Shader、不自动创建 Local Var | 定向 `70 passed`、全量 `313 passed, 3 skipped`；团结 2022.3.62t13 + ASE 1.9.6.2 的真实 248 节点图（230 个活动节点参与布局）只读 V3 审计零硬失败且未写盘；隔离副本空路由事务确认真实 API 探测、`SaveToDisk` 与结构回读，原 Shader 哈希不变；`v0.6.1@9cdc1ee` 经 run 34045894959、六项正式资产、本机安装和隔离回滚验证完成私有发布；新增/移动锚点和正常缩放视觉仍待实机签收 |
| CR-0022 | 将许可证改为 MIT，并把 GitHub 仓库转为公开源码分发 | 交付链 README LICENSE | 不改 CLI 行为或版本号；历史私有 Release 记录保持原样；不上传 PyPI/CLI Hub | REG-0021 | 恢复专有 LICENSE 与私有可见性需单独授权 | 用户 2026-09-07 明确要求开源以便同事安装 |
| CR-0023 | 将 PyPI 项目 `asecli` 作为规范安装入口，并以 Trusted Publishing 在版本 tag 后上传 | 交付链 README pyproject CI | 新增 `uv tool install asecli`；不改 CLI 行为；不以普通 push 发版；不上 CLI Hub | REG-0021 | 从 PyPI 撤回该版本并回退 README 安装入口 | 用户 2026-09-07 要求做成最终、最常见安装形态 |
| CR-0024 | 将 EditorGraphSpec 加性升级为 v3：版本化 primitive 闭包、recipe 原生展开/逐节点 fallback，以及属性 precision/default/min/max | MOD-BRIDGE MOD-CLI | v1/v2 解析和文本默认不变；仅支持 ASE 1.9.6.2 与已验证 URP Unlit 模板；SGCLI 仍是独立项目 | REG-0053 | 不接受 v3 即回退；未发布前不迁移既有 Shader，不把整图降级为黑盒 | 自动契约通过；隔离团结 2022.3.61t9 + ASE 1.9.6.2 创建/全新进程重载通过，0 Shader/CS error、0 staging；尚未发布 |
| CR-0025 | 将当前规范分发入口确定为公开 GitHub Release，并把 PyPI 发布延后到具备账号且再次明确授权时 | 交付链 README scripts CI | 一键安装继续解析最新 GitHub Release wheel；PyPI job 默认关闭；不移动既有 tag，不改 CLI 行为，不上 CLI Hub | REG-0021 REG-0057 | 恢复 PyPI 前先配置匹配仓库/workflow/environment 的 Trusted Publisher，再显式启用 job | 用户说明没有 PyPI 账号并明确要求继续 GitHub Release 发布；`v0.6.2` 公开资产、本机安装和回滚已验证 |
| CR-0026 | 恢复 PyPI 为规范包安装入口，并以 GitHub OIDC Trusted Publishing 上传与 GitHub Release 相同的 `v0.6.2` wheel/sdist | 交付链 README CI PyPI | 设置 `ASECLI_PYPI_ENABLED=true`；Publisher 精确绑定 `seeseeczl/ASECLI`、`publish.yml`、environment `pypi`；不保存或使用 API Token；GitHub Release 入口保持有效 | REG-0021 REG-0052 REG-0057 | 关闭仓库变量并在 PyPI yank 受影响版本；保留 GitHub Release 回滚入口 | 用户取得 PyPI 账号并完成 GitHub 连接后明确要求继续配置；run 34192454152 attempt 3 发布成功，PyPI 匿名摘要与隔离安装通过 |
| CR-0027 | 将随 wheel 分发的 ASECLI Skill 从 Codex 默认安装扩展为主流 Agent Skills 通用安装 | MOD-CLI MOD-SKILL | 新增 `--agent`、`--scope`、`--project-root`；开放 `.agents/skills` 覆盖 Codex/Cursor/Gemini CLI/GitHub Copilot，Claude Code 使用 `.claude/skills`；无参数与 `--skill-root` 保持兼容；`all` 先检查所有内容冲突 | REG-0058 | 禁用新路由参数即可退回 CR-0015 的 Codex 默认目录；不删除或覆盖已安装 Skill | 定向 46 passed、Skill validator、Project Architect strict 与隔离 wheel 安装通过；BUG-0023 已修复固定片段编译与 SHA 回归；全量 358 passed/3 skipped；发布追溯 REL-0013 |
| CR-0028 | 补齐 SG JSON 创建后的 ASE 图整理工作流：端口级复用计划、折叠接口计算基线、刚性计算岛排布与分层验收 | MOD-CORE MOD-LAYOUT MOD-CLI MOD-SKILL | 只读 graph-review；fanout 必须显式选择，默认消费组规则不变；island-columns 默认 1；不重建现有 Shader，不修改 SGCLI，不猜动态节点字段 | REG-0060 REG-0061 | 不用新增参数即保留原布局；回退本任务文件增量，不覆盖并行创建器改动；不发布 | 自动验证中；Local Var 自动 Editor 创建未实现，画布/GUI/渲染验收待具体目标，不以只读计划冒充写入 |

授权来源：用户于 2026-09-01 明确要求“执行优化任务，全部完成”，并于同日批准吸收 ASE Editor API 方案后要求“执行”；随后明确要求先推送并执行对抗性审计优化计划。2026-09-02 用户要求推送并进一步明确“以正式的 CLI 形式发布”，据此创建私有正式 GitHub Release `v0.1.0`；同日进一步明确说明条样式一致性必须成为 CLI 的规范能力，据此登记 CR-0011/BUG-0015，并明确所有通过 CLI 构建的 ASE 文件都必须使用中文属性显示名、自动变量名与默认值 Tooltip 和中文使用说明，据此登记 CR-0012。同日用户要求执行 `2026-09-02-211218` 优化计划，据此登记 CR-0013；随后书面授权打 tag 并发布 `v0.2.0`。2026-09-03 用户要求执行最新优化任务书并把剩余门槛全部完成，据此登记 CR-0014 并授权完成必要提交、直接推送与远程 CI 验证；随后明确要求“推送并发布”，据此授权创建私有正式版本 `v0.3.0`。2026-09-04 用户要求将 ASE 精排与 Inspector 编排规范作为可安装 Skill 并授权发布 `v0.3.1`。2026-09-05 用户要求执行 MZGUI 对接与效率优化，随后分别授权私有版本 `v0.4.0`、`v0.4.1` 和审计补丁 `v0.4.2`。2026-09-06 用户明确 Tooltip 必填、HelpBox 可选、条件置灰并授权 `v0.5.0`；随后要求脑图式 DAG 精排并授权 `v0.6.0`；又将 Local Var 门槛改为两个及以上不同消费组，据此登记 CR-0020；最后以多组人工示范明确递归局部鱼骨、Comment 保留和显式 WireNode 路由并要求执行，据此登记 CR-0021。2026-09-07 用户明确授权提交、推送、私有 Release `v0.6.1` 和本机安装。同日进一步明确要求开源该项目并提供同事安装命令，据此登记 CR-0022：采用 MIT 许可证并将 GitHub 仓库转为 public。随后要求做成最终、最常见的安装形态，据此登记 CR-0023：发布 PyPI 项目 `asecli`，用户命令为 `uv tool install asecli`；仍未授权 CLI Hub。2026-09-08 用户进一步明确要求推送、发布新版本并安装本机，据此授权 `v0.6.2` 的 main 推送、tag、GitHub Release、PyPI Trusted Publishing 与本机正式产物安装；CLI Hub 仍不在授权范围。首次 PyPI OIDC 因没有对应账号和 Trusted Publisher 失败后，用户说明没有 PyPI 账号并明确要求继续 GitHub Release 发布，据此以 CR-0025 将 PyPI 延后并完成 GitHub-only 交付。随后用户取得 PyPI 账号并明确要求继续配置，据此登记 CR-0026，启用精确匹配的 Pending Publisher 并通过 run 34192454152 attempt 3 完成首次 PyPI 发布。同日用户要求将现有 Skill 改为主流 Agent 通用，据此登记 CR-0027；本次不包含推送、tag 或发布。
